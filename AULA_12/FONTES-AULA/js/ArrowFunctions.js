/*function maior ( a , b )
{
    if ( a > b )
    {
        return ( a + " é maior que " + b );
    }
    else
    {
        return ( b + " é maior que " + a )
    }
}*/

/*const maior = ( a , b ) =>
{
    return a > b ? `${a} é maior que ${b}` : `${b} é maior que ${a}`;
}*/

const maior = ( a, b ) => a > b ? `${a} é maior que ${b}` : `${b} é maior que ${a}`;

console.log( maior( 20 , 35 ) );